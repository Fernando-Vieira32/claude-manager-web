// Manifesto dos painéis. Adicionar um recurso novo à interface = 1 import + 1 item.
// (ex.: no futuro) import editor from './editor.js';

import newConversation from './new-conversation.js';
import sessions from './sessions.js';
import conversations from './conversations.js';
import trash from './trash.js';
import services from './services.js';

export default [newConversation, sessions, conversations, trash, services];
